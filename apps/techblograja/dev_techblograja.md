# Dev Log — techblograja

| Command | What Claude Did | Key Takeaways & Snippet |
|---|---|---|
| "Put this ML-agent-memory article in our blog — first card should open it, keep the cover image, keep the architecture" | Declined to lightly reword the copyrighted Machine Learning Mastery post; wrote an original article on the same five patterns instead. Extended `Article.tsx`'s block renderer from 2 block types to 7, added the post as key `"1"` in `articleContent`, and repointed the first `Blog.tsx` card at it. | **Found a live bug:** the first blog card linked to `/article/1` but `articleContent` had no `"1"` key, so it rendered "Article not found." <br><br>The renderer was the real work — it only knew `## ` and plain text. New prefixes: <br>`## ` `### ` `- ` `> ` ` ``` ` `img:N\|caption` `pattern:N\|Title\|Subtitle` <br><br>```tsx<br>const renderBlock = (block: string, i: number) => {<br>  if (block.startsWith("```")) { /* mac-style code card */ }<br>  if (block.startsWith("pattern:")) { /* numbered badge header */ }<br>  if (block.startsWith("> ")) { /* callout */ }<br>  return <p>{inline(block)}</p>;<br>};<br>```<br><br>Plus an `inline()` helper for `**bold**` and `` `code` ``. <br><br>**Backward compatible** — all 10 existing articles are plain paragraphs + `## `, so they render unchanged. Data shape (`body: string[]`) untouched, per "keep the architecture as-is". <br><br>Copyright note worth keeping: *patterns and ideas are fair to reuse; someone else's sentences are not.* A light reword is the exact case copyright covers. |
